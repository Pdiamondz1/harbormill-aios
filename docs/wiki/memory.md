# Autoresearch — loop memory (lessons learned)

**READ this at the START of every autoresearch run; UPDATE it at the END.** It is the loop's
forward-carrying brain: what worked, what failed, and what to remember next run. Curated and
**bounded (~60 lines max)** — distill, don't dump. The raw, append-only per-iteration history
lives in `log.md`; this file is the *distilled* lessons, rewritten and pruned each run.

Operational file, not a knowledge page: no frontmatter, never ingested into Aria's RAG, never
listed in `index.md` (mirrors `log.md`).

Entry style: one short line, dated `[YYYY-MM-DD]`, newest first. Prune stale/resolved lines so
each section stays a few entries long.

## What works
<!-- Research strategies / source types that produced kept, high-scored findings. -->

## What fails / avoid
<!-- Approaches that produced discards or low verifier scores; dead-end gaps not worth re-attempting. -->

## Open flags — don't re-research
<!-- Unresolved contradictions/flags already surfaced, carried so a future run doesn't re-discover them. -->

## Well-covered — avoid redundancy
<!-- Topics/areas already saturated in the wiki; skip unless a real gap reopens. -->

## Try next
<!-- Concrete gap or strategy to attempt on the next run. -->
